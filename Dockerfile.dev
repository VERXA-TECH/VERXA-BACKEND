FROM node:20-bookworm-slim

WORKDIR /usr/src/app

# Install pnpm via corepack (already bundled in Node 20+)
RUN corepack enable

COPY package.json pnpm-lock.yaml ./

RUN pnpm install --frozen-lockfile

COPY . .

CMD ["pnpm", "run", "dev"]